{{!< default }}

<header>
    <div class="container">
        <div class="row">
            <div class="col-lg-12">
                <div class="intro-text">
                    <span class="name">{{@blog.title}}</span>
                    <hr class="star-light">
                    <span class="skills">{{@blog.description}}</span>
                </div>
            </div>
        </div>
    </div>
</header>

<section>
    <div class="container">
        <div class="row">
			{{> "post" }}
		</div>
        {{pagination}}
    </div>
</section>

